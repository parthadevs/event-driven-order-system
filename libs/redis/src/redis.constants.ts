/**
 * Token used to inject the Redis client natively.
 */
export const REDIS_CLIENT = Symbol('REDIS_CLIENT');

/**
 * Token used for module options if needed later.
 */
export const REDIS_MODULE_OPTIONS = Symbol('REDIS_MODULE_OPTIONS');